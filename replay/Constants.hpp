#pragma once

#include <string>

namespace WebGPUNativeReplay {

const uint32_t VERSION_MAJOR = $VERSION_MAJOR;
const uint32_t VERSION_MINOR = $VERSION_MINOR;
const uint32_t FILE_VERSION = $FILE_VERSION;
const std::string DAWN_BRANCH = $DAWN_BRANCH;
const std::string DAWN_COMMIT = $DAWN_COMMIT;
const std::string WGPU_TAG = $WGPU_TAG;
const std::string WGPU_COMMIT = $WGPU_COMMIT;

}
